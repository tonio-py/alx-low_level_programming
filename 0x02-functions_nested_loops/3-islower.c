#include "main.h"

/**
 * _islower- used to check if the letter is lowercase or not`
 * @c: takes integer input
 *
 * Return: 1 if success and 0 if fail
 */
int _islower(int c)
{
	if (c > 96)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

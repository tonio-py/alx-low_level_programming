#include "main.h"

/**
 * mul - solves for the product of two numbers
 * @a: takes a as an integer
 * @b: takes b as an integer
 * Return: returns the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
